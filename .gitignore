# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables (secrets — never commit)
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# os / editor
.DS_Store
.idea/

# Imágenes placeholder / fotos de terceros en public/hero (copyright) —
# NO subir. Se conserva solo el fondo de marca que usa el Hero.
/public/hero/*
!/public/hero/hero-key-background.jpg

# Claude Code: ajustes locales de la máquina (no compartir)
.claude/settings.local.json
