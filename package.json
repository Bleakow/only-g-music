{
  "name": "@only-g/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "typecheck:native": "turbo run typecheck:native",
    "test": "turbo run test",
    "gen:world-map": "node scripts/generate-world-map.mjs",
    "format": "pnpm -r --if-present run format"
  },
  "devDependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "turbo": "^2",
    "world-atlas": "^2.0.2"
  }
}
