# G Notes — Web

Escritor inteligente para compositores (app hermana de Only G Music).

**Estado:** placeholder cableado al workspace. Pendiente de scaffolding real
(Next 15) en tanda posterior: reusar `@only-g/auth` (mismo uid/Firebase),
`@only-g/ui` (tokens + glass), y consumir `@only-g/ai-services`.
